global:
  scrape_interval: 5s # 5초마다 데이터 수집 (부하 테스트 시 촘촘하게 보기 위함)

scrape_configs:
  - job_name: 'moa-spring-boot'
    metrics_path: '/actuator/prometheus'
    static_configs:
      # Docker 컨테이너 내부에서 호스트(내 PC)에 띄워진 스프링 부트(8080)를 찌르기 위한 주소
      - targets: ['host.docker.internal:8080']